// Bridge module used by the refactored explore systems.
// The actual menu renderer still lives in the command module so existing
// interaction flow stays unchanged.
export { showExploreMenu } from '../../commands/explore';
